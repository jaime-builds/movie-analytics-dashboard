diff --git a/docs/audits/app-audit-2026-07-05.md b/docs/audits/app-audit-2026-07-05.md
index 6b29a5d..4784282 100644
--- a/docs/audits/app-audit-2026-07-05.md
+++ b/docs/audits/app-audit-2026-07-05.md
@@ -19,7 +19,7 @@ Recommended next step: fix the security and migration-health items first, then a

 ## Fix Pass Status - 2026-07-05

-This report began as a pre-fix audit. The code has since had two targeted fix passes, so the findings below should be read as the original audit record plus this status update.
+This report began as a pre-fix audit. The code has since had three targeted fix passes, so the findings below should be read as the original audit record plus this status update.

 Round 1 fixed:

@@ -40,10 +40,15 @@ Round 2 fixed:
 - Updated `002_expand_password_hash` to use Alembic batch alteration for SQLite compatibility.
 - Removed the `/api/v1/movies` rate-limit exemption by applying a route-specific limit and added regression coverage with rate limiting enabled.

-Still open after Round 2:
+Round 3 fixed:
+
+- Added Alembic/model indexes for `cast.movie_id`, `cast.person_id`, `crew.movie_id`, `movie_companies.company_id`, `movies.release_date`, `movies.vote_average`, and `movies.popularity`.
+- Checked the local SQLite dev database before adding review uniqueness; found `duplicate_pairs=0` and `extra_duplicate_rows=0`.
+- Added a named unique constraint for one review per `(user_id, movie_id)` using Alembic batch alteration for SQLite compatibility.
+- Added migration coverage that builds a clean SQLite database with `alembic upgrade head`, asserts the new indexes and review uniqueness constraint exist, and confirms duplicate reviews fail at the database layer.
+
+Still open after Round 3:

-- Missing high-value indexes for several hot query paths.
-- Review uniqueness is still enforced only in application code.
 - Broader test/CI gaps remain, including ignored legacy tests and limited lint/security scope.
 - Deployment file drift, scheduler drift, sync log-directory robustness, aggregate average handling, production security defaults, relationship-query efficiency, legacy import stats, runtime API docs, and broader README/TODO drift remain open.

diff --git a/src/models.py b/src/models.py
index a20d73e..94bf116 100644
--- a/src/models.py
+++ b/src/models.py
@@ -14,6 +14,7 @@ from sqlalchemy import (
     String,
     Table,
     Text,
+    UniqueConstraint,
     create_engine,
 )
 from sqlalchemy.ext.declarative import declarative_base
@@ -40,6 +41,7 @@ movie_companies_table = Table(
     Base.metadata,
     Column("movie_id", Integer, ForeignKey("movies.id"), primary_key=True),
     Column("company_id", Integer, ForeignKey("production_companies.id"), primary_key=True),
+    Index("idx_movie_companies_company_id", "company_id"),
 )

 collection_movies_table = Table(
@@ -128,6 +130,9 @@ class Movie(Base):
     __table_args__ = (
         Index("idx_movies_vote_count", "vote_count"),
         Index("idx_movies_title", "title"),
+        Index("idx_movies_release_date", "release_date"),
+        Index("idx_movies_vote_average", "vote_average"),
+        Index("idx_movies_popularity", "popularity"),
     )

     # Relationships
@@ -208,6 +213,7 @@ class Review(Base):

     # Index for efficient queries
     __table_args__ = (
+        UniqueConstraint("user_id", "movie_id", name="uq_reviews_user_movie"),
         Index("idx_movie_reviews", "movie_id"),
         Index("idx_user_reviews", "user_id"),
     )
@@ -276,6 +282,11 @@ class Cast(Base):
     character_name = Column(String(255))
     cast_order = Column(Integer, default=0)

+    __table_args__ = (
+        Index("idx_cast_movie_id", "movie_id"),
+        Index("idx_cast_person_id", "person_id"),
+    )
+
     # Relationships
     movie = relationship("Movie", back_populates="cast_members")
     person = relationship("Person", back_populates="cast_roles")
@@ -294,6 +305,7 @@ class Crew(Base):
     department = Column(String(100))

     __table_args__ = (
+        Index("idx_crew_movie_id", "movie_id"),
         Index("idx_crew_person_id", "person_id"),
         Index("idx_crew_person_job", "person_id", "job"),  # Covers director lookups
     )
diff --git a/tests/test_migrations.py b/tests/test_migrations.py
index 8b98bd1..927e35c 100644
--- a/tests/test_migrations.py
+++ b/tests/test_migrations.py
@@ -7,7 +7,10 @@ import subprocess
 import sys
 from pathlib import Path

+import pytest
 from sqlalchemy import create_engine, inspect
+from sqlalchemy.exc import IntegrityError
+from sqlalchemy.sql import text


 def test_alembic_upgrade_head_builds_clean_sqlite_database(tmp_path):
@@ -40,3 +43,42 @@ def test_alembic_upgrade_head_builds_clean_sqlite_database(tmp_path):

     user_columns = {column["name"]: column for column in inspector.get_columns("users")}
     assert user_columns["password_hash"]["type"].length == 256
+
+    expected_indexes = {
+        "cast": {"idx_cast_movie_id", "idx_cast_person_id"},
+        "crew": {"idx_crew_movie_id", "idx_crew_person_id", "idx_crew_person_job"},
+        "movie_companies": {"idx_movie_companies_company_id"},
+        "movies": {
+            "idx_movies_release_date",
+            "idx_movies_vote_average",
+            "idx_movies_popularity",
+        },
+    }
+    for table_name, index_names in expected_indexes.items():
+        actual_index_names = {index["name"] for index in inspector.get_indexes(table_name)}
+        assert index_names <= actual_index_names
+
+    review_constraints = {
+        constraint["name"]: constraint["column_names"]
+        for constraint in inspector.get_unique_constraints("reviews")
+    }
+    assert review_constraints["uq_reviews_user_movie"] == ["user_id", "movie_id"]
+
+    with engine.begin() as connection:
+        connection.execute(
+            text("INSERT INTO users (id, username, password_hash) VALUES (1, 'u1', 'hash')")
+        )
+        connection.execute(text("INSERT INTO movies (id, tmdb_id, title) VALUES (1, 101, 'M1')"))
+        connection.execute(
+            text(
+                "INSERT INTO reviews (user_id, movie_id, content) " "VALUES (1, 1, 'first review')"
+            )
+        )
+
+        with pytest.raises(IntegrityError):
+            connection.execute(
+                text(
+                    "INSERT INTO reviews (user_id, movie_id, content) "
+                    "VALUES (1, 1, 'second review')"
+                )
+            )
